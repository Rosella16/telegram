spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/telegram-message
    username: postgres
    password: rosella22
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080

logging:
  level:
    root: INFO
